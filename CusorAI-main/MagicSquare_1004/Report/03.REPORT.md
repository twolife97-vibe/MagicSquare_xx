# MagicSquare_1004 STEP 1 — Mom Test 인터뷰 + 세션 3 워크북 보고서

| 항목 | 내용 |
|------|------|
| 프로젝트 | `c:\DEV\MagicSquare_1004` |
| 단계 | STEP 1 — Mom Test 인터뷰 → 세션 3 워크북(R-G-I-O) |
| 보고서 생성일 | 2026-06-05 |
| 멘토 스타일 | Rob Fitzpatrick — The Mom Test |
| 페르소나 | 4×4 부분 마방진(빈칸 2개) 학습자 |

---

## 1. 요약

| 구분 | 결과 |
|------|------|
| 인터뷰 질문 | 2회 (질문 1 + 추궁 1) |
| Mom Test 적합 답변 | **1건** |
| 솔루션 혼입 답변 | 0건 |
| 워크북 채점 | **7.5 / 10** |
| **판정** | **부분 검증** — 과거·구체 행동 1건 확보, R-G-I-O·세션 3 범위 확정 |

**Good Data 1건**으로 **진짜 문제** 1차 확정. 대각선 누락·20분 비용 확인. **R-G-I-O**·**주제**·**성공 기준 3개**를 Mom Test 증거에 연결해 세션 3 범위(Rule · Command · Test Loop)를 고정.

---

## 2. 페르소나

| 항목 | 내용 |
|------|------|
| 역할 | 4×4 **부분** 마방진 학습자 |
| 행동 범위 | 손으로 풀기 / 코드로 다루기 |
| 도메인 | 4×4 격자, **빈칸 2개(0)**, **1~16**, **합 34** |
| 10선 | 행 4 + 열 4 + 주대각 1 + 부대각 1 |

### 예시 격자 (과제 기준)

| | | | |
|---|---|---|---|
| 16 | 3 | 2 | 13 |
| 5 | 10 | 11 | **?** |
| 9 | 6 | **?** | 12 |
| 4 | 15 | 14 | 1 |

---

## 3. 인터뷰 진행 기록

### 3.1 질문 1

> 마지막으로 4×4 부분 마방진(빈칸 2개, 합 34)을 **손으로 풀거나 코드로 다룬 게** 언제였어요? 그때 **처음 뭘 하려다가** 시작했는지, **처음부터 끝까지** 순서대로 말해 주세요.

**응답:**

> “지난주 OO 과제에서 빈칸 2개 넣고 행·열·대각선 합 맞췄는데 대각선 하나를 빼먹어서 20분 날렸다.”

| Mom Test 평가 | 결과 |
|---------------|------|
| 사실 | ✅ (본인 경험) |
| 과거 | ✅ (“지난주 OO 과제”) |
| 구체 | △ (빈칸 2개, **20분**, **대각선 1개 빼먹음** ✅ / 20분 **동안 행동** △) |
| 솔루션 혼입 | ✅ 없음 |

**종합:** Mom Test 기준 **Good Data**.

### 3.2 추궁 질문 1

> 그 **20분 동안** 구체적으로 뭐 하고 있었어요? **행·열만** 다시 세고 있었는지, **빈칸 값**부터 바꿔 가며 시도했는지 말해 주세요.

**응답:** *(미응답 — 인터뷰 종료)*

---

## 4. Mom Test 분석

### 4.1 표면 문제 (잘못된 정의 — 솔루션 혼입)

Mom Test에서 **문제 정의에 쓰면 안 되는** 문장들:

- “4×4 마방진을 **BCE로 분류**하고 `MagicSquare` / `Solver` / `GridUI` **프로그램부터 만든다**.”
- “빈칸 2개를 **Solver 알고리즘**으로 **자동 채우는 프로그램**을 먼저 만든다.”
- “**GridUI** / PyQt **UI 프로그램**을 설계하는 것이 문제다.”
- “**실패 조건 3개 도출** + BCE 설계만 하고 **테스트 없이** 끝낸다.”

→ **‘무엇을 만들지’** 만 있고, **불편·비용·과거 행동**이 없음.

### 4.2 진짜 문제 (한 문장)

> 4×4 부분 마방진에서 빈칸 2개를 채운 뒤 **행·열·대각선(10선) 합 34 여부를 확인할 때 일부 줄(예: 대각선)을 빼먹어 맞는지 판정하지 못하고**, 같은 시도를 반복하며 **20분** 같은 시간을 잃는다.

### 4.3 Mom Test 증거 3줄

| # | 인용 | Mom Test 적합성 |
|---|------|-----------------|
| 1 | “**지난주 OO 과제에서** 빈칸 2개 넣고 행·열·대각선 합 맞췄는데” | ✅ **과거·행동** |
| 2 | “**대각선 하나를 빼먹어서**” | ✅ **구체·실수** |
| 3 | “**20분 날렸다.**” | ✅ **비용** |

---

## 5. 워크북 채점

| 체크 | 판정 | 근거 |
|------|------|------|
| 미래 가정("~하면 좋겠다") 없음 | ✅ | 전 항목 과거·사실 서술 |
| 과거 행동·시간·실수 구체성 있음 | △ | 20분·대각선 ✅ / 20분 동안 행동 △ |
| 진짜 문제에 솔루션名(TDD/PyQt/Cursor) 없음 | ✅ | 진짜 문제 문장에 없음 |
| 표면 문제와 진짜 문제가 분리됨 | ✅ | 만들기 vs 판정·확인·시간 |
| MagicSquare 도메인(4×4, 10선, 34, 빈칸 2개) 반영 | △ | 진짜 문제 ✅ / 증거 인용에 10선·34 △ |

**점수: 7.5 / 10**

---

## 6. 수집된 불편·비용·행동

| 항목 | 내용 |
|------|------|
| **불편** | 행·열·대각선 합 맞췄다고 생각했는데 **대각선 하나를 빼먹어** 맞는지 확신 못 함 |
| **비용** | **20분** 낭비 |
| **마지막 행동** | OO 과제에서 **빈칸 2개 채우고** 행·열·(일부) 대각선 합 확인 — **대각선 1개 검증 누락** |

---

## 7. 세션 3 워크북 (Mom Test → R-G-I-O)

### 7.1 주제 (1문장)

> **4×4 부분 마방진에서 빈칸을 채운 직후, 행·열·대각선 10선이 각각 34인지 빠르게 확정하고, 틀리면 어느 줄인지 바로 짚을 수 있게 검증 규칙과 테스트 루프를 만든다.** 미완성 격자(0 포함)는 `incomplete`로 구분한다.

*(솔루션 최소화 — 앱·솔버·UI가 아니라 **판정·확인 비용**만 다룸)*

### 7.2 R-G-I-O

| | 내용 |
|---|---|
| **R — Role** | 4×4 **부분** 마방진 학습자. 빈칸 2개(0)를 1~16으로 채운 뒤 **맞았는지 스스로 확인**해야 함 |
| **G — Goal** | 빈칸 채운 후 **10선 합 34 여부를 즉시 판정**하고, 틀리면 **어느 행·열·대각선**인지 식별 *(20분 낭비 → 처음·한 번에)* |
| **I — Input** | `grid: list[list[int]]` — 4×4 정수 격자. 셀 값 **0**(빈칸) 또는 **1~16** |
| **O — Output** | `validate_lines(grid)` → **ValidationResult**: `ok` (bool), `status` (`"pass"` \| `"fail"` \| `"incomplete"`), `lines[]` — fail 시 **틀린 줄만** `{ id, sum, expected: 34 }`. 줄 ID: `row:0`~`row:3`, `col:0`~`col:3`, `diag:main`, `diag:anti`. **R5:** 격자에 `0`이 하나라도 있으면 `status=incomplete`, 합 계산·34 비교는 수행하지 않음 |

### 7.3 성공 기준 3개 (Mom Test 증거 연결)

| # | 성공 기준 | 연결 증거 |
|---|-----------|-----------|
| 1 | **`validate_lines(grid)`** 한 번으로 **10선×34 검증**을 수행한다 (행 4 + 열 4 + 대각 2) | “행·열·**대각선** 합 맞췄는데 **대각선 하나를 빼먹어서**” |
| 2 | Test Loop **Red→Green**: Red — 합≠34 → `ok=false`, `status=fail`; Red — `0` 포함 → `status=incomplete` (T3); Green — 정답 격자 → `ok=true`, `status=pass` (T1) | “**34가 안 맞아 20분** 날렸다” → 같은 실패를 테스트로 고정 |
| 3 | fail 시 `lines[]`에 **틀린 줄 ID**(`row:2`, `diag:main` 등)와 `sum`, `expected: 34`를 반환한다 (10선 전부 검사, 줄 누락 없음) | “맞는지 **판정하지 못하고**” 같은 시도 반복 → **줄 단위**로 짚어야 함 |

### 7.4 표면 문제 — 이번 프로젝트에서 하지 않을 것

| 하지 않음 | 이유 (Mom Test) |
|-----------|-----------------|
| BCE 전체 / `GridUI` / `InputHandler` | UI·입력은 **표면 솔루션** — 이번 고통은 **판정·확인** |
| `Solver` / 빈칸 자동 채우기 / 힌트 | 학습자 고통은 **풀기**보다 **맞는지 확인** |
| BCE 분류·클래스 설계만 하고 테스트 없음 | 과제는 **설계표** — 검증·재현 없음 |
| 1~16 중복·범위 검증까지 한꺼번에 | 세션 3 범위 밖 — **합 34 판정**에 집중 |

### 7.5 8계층 — 이번 세션에서 만들 것만

| 계층 | 세션 3에서 할 일 | Mom Test 연결 |
|------|------------------|---------------|
| **Rule** | R1~R5: **행·열·대각선 합 = 34** 판정. R5 — 0(빈칸) 포함 시 `status=incomplete` (**fail과 구분**, 합 검증 생략) | “34가 안 맞았는지” **판정 기준**을 코드·문서로 고정 |
| **Command** | `validate_lines(grid)` → `ValidationResult` — 10선 합 계산 + 34 비교 + fail 시 **틀린 줄 ID·sum·expected** 반환 | 합 확인에 **시간 많이** → **한 Command**로 대체 |
| **(Skill)** | *(선택)* pytest 실행 / 격자 fixture 준비 Skill | 매번 손으로 10선 더하기 제거 |
| **Test Loop** | Red: 합≠34 → `ok=false`, `status=fail`, **실패 줄** assert; Red: `0` 포함 → `status=incomplete` (T3); Green: 정답 격자 → `ok=true`, `status=pass` (T1) | **20분 낭비** → **같은 실패를 1번에 재현** |

**이번 세션에서 만들지 않음:** Entity(`MagicSquare`/`Cell`), Boundary(`ResultDisplay`), Solver, MissingFinder, 전체 BCE

---

## 8. 결론 및 다음 단계

### 결론

- **Good Data 1건** 확보: 대각선 누락 + **20분** 비용.
- **진짜 문제** 1차 확정: **10선 합 34 판정·확인** 비용과 **줄 누락** 리스크.
- **세션 3 워크북** 확정: 주제 · R-G-I-O · 성공 기준 3개 · Rule/Command/Test Loop 범위.

### 권장 액션

1. **추가 인터뷰(선택)** — 20분 동안 한 행동 1회 (추궁 미응답 보완)
2. **Rule + Command + Test Loop 구현** — `validate_lines` 우선
3. **Solver / UI / BCE 전체** — 세션 3 이후

---

## 9. 관련 문서

| 문서 | 설명 |
|------|------|
| [01.REPORT.md](./01.REPORT.md) | STEP 1 Mom Test 인터뷰 (7.5/10) |
| [02.REPORT.md](./02.REPORT.md) | STEP 1 보완 — 역할 분리 Mom Test 시뮬레이션 (8.5/10) |
| [Prompting/03.Export-Transcript.md](../Prompting/03.Export-Transcript.md) | 본 세션 대화 Export |

---

*본 문서는 `Report/03.REPORT.md` — MagicSquare_1004 STEP 1 Mom Test + 세션 3 워크북 보고서입니다.*
