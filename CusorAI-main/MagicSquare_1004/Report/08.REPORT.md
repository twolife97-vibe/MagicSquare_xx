# MagicSquare_1004 — Track B GREEN (D-LOC-01) + 테스트·커버리지 가이드

| 항목 | 내용 |
|------|------|
| 프로젝트 | `c:\DEV\MagicSquare_1004` |
| 단계 | 세션 5 — ARRR Respond (GREEN) |
| 보고서 생성일 | 2026-06-06 |
| 목적 | D-LOC-01 `find_blank_coords` 최소 구현·PASS 확인 및 pytest/커버리지 명령 정리 |

---

## 1. 요약

| 구분 | 결과 |
|------|------|
| Phase | **GREEN** — `src/` + `tests/entity/` |
| Layer | entity |
| Track | Logic (B) |
| Test ID | **D-LOC-01** (`FR-LOC-01`) |
| pytest (D-LOC-01) | **PASS** (1/1) |
| pytest (전체 `tests/`) | 1 PASS / 1 FAIL (`validate_lines` T2 스텁 — 무관) |
| **판정** | **GREEN 완료** — D-LOC-01 1묶음 통과 |

---

## 2. 핵심 결정·산출물

### 2.1 GREEN 진입 점검 (사전)

| 항목 | 결과 |
|------|------|
| RED 상태 | `pytest.fail` 의도적 FAIL 확인 |
| 픽스처 | `grid_g1` — G1 SSOT `[(2,3),(4,4)]` |
| `src/` 충돌 | `find_blank_coords` 미존재 — GREEN 진입 가능 |
| 좌표 오타 | 프롬프트 `[(2,2),(3,3)]` → SSOT `[(2,3),(4,4)]` 정정 |

### 2.2 구현 (`/green-minimal`)

| 항목 | 내용 |
|------|------|
| 함수 | `find_blank_coords(grid) -> list[tuple[int, int]]` |
| 파일 | `src/entity/find_blank_coords.py` (신규) |
| 알고리즘 | `GRID_SIZE` row-major 순회, `BLANK_CELL` 매칭, 1-index `(row+1, col+1)` 반환 |
| SSOT | `constants.py` — `BLANK_CELL = 0` 추가, `GRID_SIZE` 참조 |
| ECB | boundary/control import 없음, E001~E005 없음 |
| 테스트 | `pytest.fail` → `assert find_blank_coords(grid_g1) == [(2, 3), (4, 4)]` |

### 2.3 pytest 실행

```bash
python -m pytest tests/entity/test_d_loc_01.py::test_d_loc_01_blank_coords_row_major -v
# PASSED

python -m pytest tests/entity/test_d_loc_01.py -v
# PASSED (1/1)
```

### 2.4 REPL 스모크

```
G1 → [(2, 3), (4, 4)]  PASS
```

### 2.5 커버리지 (세션 중 생성)

```bash
python -m pytest tests/entity/ --cov=src --cov-report=html:htmlcov --cov-report=term-missing -v
```

| 파일 | Cover |
|------|-------|
| `find_blank_coords.py` | 100% |
| `constants.py` | 100% |
| `validate_lines.py` | 0% (스텁, entity 범위 외) |

산출: `htmlcov/index.html` (로컬 생성, git 미추적)

### 2.6 변경 파일

| 파일 | 변경 |
|------|------|
| `src/entity/find_blank_coords.py` | 신규 — FR-LOC-01 최소 구현 |
| `src/entity/constants.py` | `BLANK_CELL = 0` 추가 |
| `tests/entity/test_d_loc_01.py` | GREEN assert 교체 |

---

## 3. 다음 단계

1. **Golden** — `/golden-master` (D-LOC-01 PASS 전제, 해당 시)
2. **REFACTOR** — `/refactor-smell` → `/refactor-safe` (선택)
3. **Track B RED** — D-MIS-01 `find_not_exist_nums` (`/red-test-plan` → `/red-skeleton`)
4. **Track A (별도)** — `validate_lines` T2 GREEN (세션 3 잔여)

---

## 관련 문서

| 문서 | 설명 |
|------|------|
| [Prompting/08.Export-Transcript.md](../Prompting/08.Export-Transcript.md) | 본 세션 Transcript |
| [Report/07.REPORT.md](07.REPORT.md) | 직전 RED 스켈레톤 (D-LOC-01) |
| [docs/PRD.md](../docs/PRD.md) §10.1~10.2 | FR-LOC-01 · G1 SSOT |

---

*본 문서는 Report/08.REPORT.md — Track B GREEN (D-LOC-01) + 테스트·커버리지 가이드 세션 요약입니다.*
