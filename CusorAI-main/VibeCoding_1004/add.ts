/**
 * 두 숫자를 더합니다. 정수, 실수, 양수, 음수, 0 모두 사용할 수 있습니다.
 *
 * @example
 * add(2, 3);      // 5
 * add(-2, 3);     // 1
 * add(-2, -3);    // -5
 * add(1.5, 2.3);  // 3.8
 * add(-1.2, 0.5); // -0.7
 */
export function add(a: number, b: number): number {
  return a + b;
}
